# Supabase — launch-list capture for /api/signup
#
# The table public.cmsn_signups is insert-only under RLS: anon may INSERT and
# there is no SELECT policy, so the publishable key cannot read the list back.
# Both values are read server-side only; neither is NEXT_PUBLIC_.
#
# Find them in the Supabase dashboard under Project Settings > API.

SUPABASE_URL=https://ftmkvqfytfzovuuzzers.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Website path map

The deployed Next.js application stays at repository root: `../app`, `../public`, and `../package.json`. Do not move it merely to match this conceptual directory; deployment settings depend on these paths. PR #2 supplies the Walk-led, men-first visual consistency baseline.
